# Plain markdown, no frontmatter

This file has no YAML frontmatter, so cofr should classify it as unstructured
and skip it for state purposes. It is not an error -- the file is indexed and
scanned for ID mentions, but no domain object is created from it.
